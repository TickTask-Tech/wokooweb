# 官网环境变量示例（复制为 .env.local 用于本地，不要提交 .env.local）
# Vercel 上在 Settings → Environment Variables 里配置，不要在这里填真实 Key

# Resend 发邮件（联系表单）
# 在 https://resend.com 创建 API Key，域名/发件人需在 Resend 验证
RESEND_API_KEY=
RESEND_FROM_EMAIL=website@ticktask.co.ke
# 收件人，默认 support@ticktask.co.ke。Resend 测试模式（未验证域名）下仅能发往账号邮箱，可设 RESEND_TO_EMAIL=你的邮箱
# RESEND_TO_EMAIL=support@ticktask.co.ke

# 本地不填上述两项时，表单仍可提交，只是不会发邮件、仅在控制台打印
